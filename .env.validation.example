# Copy to .env.validation.local. Only this file is read by db:local commands.
# Never put production credentials, project references, or database URLs here.
CHAIRMAN_ENV=local
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:55431
