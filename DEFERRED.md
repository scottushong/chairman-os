# DEFERRED — Chairman이 결정해야 넘어갈 수 있는 것들

무인 실행(2026-09-05) 중 명세·시드·시안이 서로 다르게 말해서 임의로 정하지 않고 남긴 항목.
각 항목은 "무엇이 막혔나 / 왜 / 고를 수 있는 선택지" 순서다.

## 결정 (2026-09-05, Chairman)

| 항목 | 결정 | 반영 |
|---|---|---|
| D-01 EBITDA Formula | **A** — 시트값 그대로. 실 Formula는 Phase 2 ECOUNT 연동 때. | 02_기능명세 CH-008 Acceptance 문구 수정 필요(문서 쪽) |
| D-02 대기 시작 시각 | **A** — `blocked_since` 추가. 시드 1회 수정 승인. | 완료 (`tasks.json`, `types/domain.ts`, `waiting-on-me.tsx`, `data/README.md`) |
| D-03 시안 vs 시드 숫자 | **A** — 시드 유지, 시안은 레이아웃 참고. | 코드 변경 없음 |
| D-04 회사 진행률 | **C** — 라벨을 "프로젝트 진행률"로. | 완료 (`business-card.tsx`) |
| D-05 저장 위치 | **A** — Phase 1에서 DB + Audit Log 착수. | 대부분 완료 (Phase 1-B). 결정 로그 → `audit_log`, 숨김·핀 → `user_settings`. 기업 추가만 남았다 → D-08 |
| D-06 제목 중복 | 그대로 확정. | 완료 |
| D-07 차트 축 | **A** — 이중 축(좌 매출 / 우 손익 3종). | 완료 (`line-chart.tsx`, `finance-trend.tsx`) |
| D-08 기업 추가 저장 위치 | **A** — `businesses` INSERT + `audit_log('create')`를 Server Action으로. | 완료 (Phase 1-C 블록 1). `app/actions/businesses.ts`, `lib/business-id.ts`, `repository/*`. `lib/added-businesses.ts` 삭제 |
| D-09 담당자 이름 표시 | **B** — user_profiles 조인, 없으면 '미지정'. | 완료 (`repository/supabase.ts`). 실계정이 생기면 (A)로 이어진다 |

## 결정 (2026-09-07, Chairman — Phase 1-E)

| 항목 | 결정 | 반영 |
|---|---|---|
| D-15 초대 메일 | **A** — 그대로 둔다. 초대는 수동(Supabase Dashboard)으로 계정을 만들고, 권한은 `on_auth_user_created` 트리거가 붙인다. service_role은 도입하지 않는다. | 코드 변경 없음. 절차를 `docs/OPERATIONS.md`에 적는다 |

## 새로 생긴 것 (Phase 1-C, 2026-09-06)

| 항목 | 무엇이 | 급한가 | 상태 |
|---|---|---|---|
| D-10 | 결재 첨부를 넣을 경로가 없다 | 중 — 화면은 이미 첨부 자리를 보여 준다 | **해소** (Phase 1-D 블록 1, 선택지 A) |
| D-11 | 한국어 검색이 형태소가 아니라 부분 문자열이다 | 하 — 지금 데이터에서는 티가 안 난다 | 열림 |
| D-12 | Task·Project 단건 화면이 없다 | **중 → 상** — UAT TC-010 P0 Fail의 원인이다 | **해소** (Phase 1-E 블록 2, 선택지 A) |
| D-13 | CH-024 전략 좌표를 화면에서 고칠 수 없다 | 중 — 회장이 직접 쓰는 칸인데 읽기 전용이다 | **해소** (Phase 1-D 블록 1, 선택지 A) |
| D-14 | 사이드바 메뉴 13개가 아직 빈 주소다 | 하 — Phase 2 범위 | **해소** (Phase 1-D 블록 1, 선택지 B) |

## 새로 생긴 것 (Phase 1-D, 2026-09-07)

| 항목 | 무엇이 | 급한가 | 상태 |
|---|---|---|---|
| D-15 | 초대 메일을 앱에서 직접 못 보낸다 (service_role이 없다) | 중 — 사람을 늘리려면 Dashboard를 한 번 거쳐야 한다 | **결정됨** (2026-09-07, 선택지 A — 수동 초대 + 트리거 연결) |
| D-16 | 문서 3종(API·환경변수·수정가이드)이 없다 | 중 — UAT TC-025 P0 Fail. Phase 2를 기다릴 이유가 없는 유일한 Fail | 열림 |
| D-17 | Staging 환경과 Rollback 절차가 없다 | 상 — UAT TC-023·024 P0 Fail. 배포를 하려면 이게 먼저다 | 열림 |

## 새로 생긴 것 (Phase 1-E, 2026-09-07)

| 항목 | 무엇이 | 급한가 | 상태 |
|---|---|---|---|
| D-18 | 업무의 담당자·제목·마감을 Chairman OS에서 못 고친다 | 하 — 의도된 경계다. 고치려면 Layer 1과의 역할 분담을 다시 정해야 한다 | 열림 |

아래 원문은 결정 근거로 남겨 둔다.

---

## 문서 반영 필요

코드·DB에는 들어갔는데 명세 문서에는 아직 없는 것들. 문서를 고치기 전까지 명세와 구현이 다르다.

| 문서 | 무엇을 | 왜 |
|---|---|---|
| 02_기능명세 02_데이터필드 | **Task에 `blocked_since` 추가** (ISO date, 필수, 보안등급 [일반]) | DEFERRED D-02 결정 A. "이 업무가 지금 상태로 들어간 날"이다. CH-017 대기일수를 이 값에서 잰다. 시트 Tasks에는 `deadline`만 있어 대기일수를 낼 수 없었다. 이미 반영된 곳: `src/types/domain.ts`, `src/data/tasks.json`, `supabase/migrations/0001_init.sql` (tasks.blocked_since), `src/components/dashboard/waiting-on-me.tsx` |
| 02_기능명세 CH-008 Acceptance | "정의된 Formula와 일치" → "시트에 기록된 EBITDA를 그대로 표시" | DEFERRED D-01 결정 A |
| 02_데이터필드 Decision | **`attachment_url` 추가** (text, 선택, 보안등급 [제한]) | CH-041 상세 패널의 '첨부'. 사내 스토리지 링크만 둔다 — 파일 실체는 Chairman OS에 없다(CLAUDE.md 데이터 원칙). 반영: `supabase/migrations/0006_decision_attachment.sql`, `src/types/domain.ts` |
| 02_데이터필드 Document | **`storage_path` → `storage_url`**, **`uploaded_by` 추가** (uuid, [제한]) | 이름이 사실을 말해야 한다 — 이 칸에 들어가는 건 버킷 경로가 아니라 링크다. `uploaded_by`는 '링크를 등록한 사람'이고 `owner_user_id`('문서의 주인')와 다르다. 반영: `0007_documents_link.sql` |
| 05_Strategic Coordinates | **CH-024 좌표를 표로 정의** (`business_strategy`, 회사당 1행 11칸) | CH-011~014와 축이 다르다 — 저쪽은 측정되는 목표(progress_pct/deadline)고 이쪽은 사람이 쓴 방향과 판단이다. 그룹 행은 두지 않는다. 반영: `0008_business_strategy.sql`, `src/types/strategy.ts`(`BusinessStrategy`), `src/data/strategy.json`(`business_coordinates`) |


---

## D-10. 결재에 첨부를 거는 경로가 없다 (CH-041)

> **해소 (2026-09-07, Phase 1-D 블록 1) — 선택지 A.**
> 기안 화면을 만들고 거기서 첨부를 건다. `0010_decision_draft.sql`(decision_id 시퀀스),
> `app/actions/draft-decision.ts`, `components/approvals/draft-decision.tsx`.
> '내용'은 자유 서술이 아니라 `options` 여러 줄로 받는다 — 02_데이터필드에서 필수 배열이고,
> 고를 것이 없으면 결재가 아니라 보고다. 명세의 5칸에 **마감일**을 하나 더 받는다:
> 목록 정렬과 D-Day가 그 값을 쓰기 때문에 비우면 새 결재만 'D-null'로 뜬다.
> 확인: `dec_005` 발급 → 선택안 3개 · 첨부 배지 · `audit_log(create)`.

**무엇이** 0006이 `decisions.attachment_url`을 만들었고 전자결재 상세 패널이 그 자리를 그린다.
그런데 값을 **넣는 화면이 없다.** 시드에도 없어서 지금 모든 결재가 '첨부된 문서가 없습니다'로 뜬다.

**왜 이렇게 됐나** Phase 1-C의 범위는 '결재를 처리하는 것'이었다. 결재를 **올리는** 화면(CH-041의
기안 쪽)은 이번에 만들지 않았다. 첨부는 기안할 때 거는 값이라 그쪽에 붙는 게 자연스럽다.

**골라야 할 것**
- (A) 기안 화면(결재 상정)을 만들고 거기서 첨부를 건다. 권장 — 첨부만 따로 붙일 이유가 없다.
- (B) 상세 패널에서 Chairman이 나중에 링크를 붙일 수 있게 한다. 빠르지만 '결재 내용을 결재자가 고친다'가 된다.
- (C) 첨부를 CH-042 문서와 연결한다(`documents.document_id` 참조). 문서 권한이 그대로 따라오는 게 장점이지만,
  사내 스토리지 링크를 문서로 먼저 등록해야 해서 한 단계가 는다.

---

## D-11. 한국어 검색이 형태소가 아니라 부분 문자열이다 (CH-043)

**무엇이** 0009는 길을 둘 깔았다. 영문·숫자 질의는 tsvector, 한글이 섞이면 ILIKE `%…%`다.
그래서 한글 검색은 **정확도 개념이 없다** — 순위도 없고, 어절 경계도 모르고, 긴 문장 안의 우연한
겹침도 그대로 걸린다. '가격'으로 찾으면 '가격 인상'도 '판매가격표'도 같은 무게로 나온다.

**왜 이렇게 됐나** Postgres에 한국어 text search configuration이 없고 Supabase에 형태소 확장
(mecab, pg_bigm)이 올라가 있지 않다. 'simple' 설정으로 만들면 '매출'로 '매출채권'을 못 찾는다 —
회장이 검색창에 치는 말은 대개 그쪽이라 그 상태로는 검색이 아니라 함정이다.

**지금 상태로 괜찮은 이유** 검색 대상이 500행이고 전부 짧은 제목이다. trigram 색인이 붙어 있어
느리지도 않다. 문제는 데이터가 늘 때 드러난다.

**골라야 할 것**
- (A) 그대로 둔다. Phase 2에서 데이터가 늘면 다시 본다. 지금은 이걸 권한다.
- (B) Supabase에 pg_bigm을 요청해 2-gram 색인으로 바꾼다. 한국어에 맞고 순위도 나온다. 확장 승인이 선행돼야 한다.
- (C) 외부 검색 엔진(Typesense/Meilisearch)을 붙인다. 정확도는 가장 좋지만 **RLS 밖에 데이터 사본이 생긴다** —
  05_Architecture 6번 원칙과 정면으로 부딪힌다. 권하지 않는다.

---

## D-12. Task·Project 단건 화면이 없다 (CH-017 / CH-020)

**무엇이** CH-017 Waiting on Me의 Acceptance는 '원 Task와 연결'이다. 지금은 그 줄을 누르면
`/tasks?business=…&needed=1` 로 간다 — 그 업무가 **보이는 목록**까지는 가지만 그 업무 자체로는 못 간다.
검색 결과의 프로젝트도 마찬가지로 회사 상세로 보낸다.

**왜 이렇게 됐나** Phase 1-C 블록 2는 '목록 + 상태 변경'이 범위였다. 단건 화면은 댓글·이력·첨부가
같이 붙는 자리라 CH-041 상세 패널만큼의 작업이 다시 필요하다.

**골라야 할 것**
- (A) `/tasks/[id]` 를 만든다. CH-041 상세 패널과 같은 모양이면 audit_log 역조회도 그대로 재사용된다. 권장.
- (B) 목록에서 줄을 펼치는 방식(inline expand). 화면이 하나 안 늘지만 링크로 지목할 수 없다.
- (C) 그대로 둔다 — 목록에서 필터로 찾는 것으로 충분하다고 본다.

### 결정 (2026-09-07, Chairman) — **A. 단건 화면 두 개** · Phase 1-E 블록 2에서 구현

`/tasks/[id]` 와 `/projects/[id]` 를 만들었다. 이제 업무 하나를 링크로 지목할 수 있다.

**무엇이 어디로 연결됐나**

| 어디서 | 예전 | 지금 |
|---|---|---|
| 대시보드 Waiting on Me | `/tasks?business=…&needed=1` (필터된 목록) | `/tasks/[id]` (그 업무) |
| 업무 목록의 제목 | 링크 없음 | `/tasks/[id]` |
| 검색 결과 · 업무 | `/tasks?business=…` | `/tasks/[id]` |
| 검색 결과 · 프로젝트 | `/business/[id]` (회사 상세로 우회) | `/projects/[id]` |
| 업무 상세 → 프로젝트 | 없음 | `/projects/[id]` |
| 프로젝트 상세 → 업무 | 없음 | `/tasks/[id]` |

**이력은 audit_log 역조회다.** 업무에 별도의 이력 테이블을 만들지 않았다 — 감사 기록이 곧 이력이다.
`listEntityAudit(entity_table, entity_id)` 하나를 repository 계약에 더했고, 두 화면이 같은
`AuditTimeline`으로 그린다. 전건을 읽어 화면에서 거르지 않는다(업무가 늘면 매 요청이 무거워진다).

**담당자는 보여 주기만 한다.** 고치는 버튼을 두지 않았다. `TaskPatch`가 상태와 회장확인 두 칸만
받도록 되어 있고, 그 이유가 repository 주석에 이미 적혀 있다 — *"이 화면은 '지금 어떻게 되고
있나'를 옮기는 자리지 업무를 편집하는 자리가 아니다. 편집은 Business OS(Layer 1)의 일이다."*
담당자 변경을 넣으려면 그 경계를 먼저 옮겨야 하고, 그건 D-12가 아니라 별건이다 → **D-18**.

**프로젝트 상세에는 고치는 자리가 아예 없다.** repository 계약에 프로젝트를 고치는 함수가 없다.
없는 버튼을 그려 두면 눌렀을 때 실패하는 화면이 된다.

**남는 것** 전사 프로젝트 목록(`/projects`)은 여전히 없다. 사이드바의 '프로젝트'는 아직
`/coming-soon`이다 — 단건은 섰지만 목록은 Phase 2 범위 그대로다.

---

## D-13. CH-024 전략 좌표를 화면에서 고칠 수 없다

> **해소 (2026-09-07, Phase 1-D 블록 1) — 선택지 A.**
> 칸별 인라인 편집. `lib/strategy-fields.ts`(열한 칸의 단일 정의),
> `app/actions/strategy.ts`(한 번에 한 칸), `components/business/coordinates-panel.tsx`.
> 저장은 upsert다 — CH-002로 방금 만든 회사에는 좌표 행이 없어서, update로 두면
> 그 회사는 첫 문장을 영영 못 쓴다. 권한 판정은 옮기지 않았다: 0008의
> `business_strategy_write`가 `can_approve()`로 Chairman·BusinessCEO만 통과시킨다.
> 확인: biz_vana Bottleneck 수정 → `audit_log`에 바뀐 칸만 before/after로 기록.

**무엇이** 0008 `business_strategy`는 회장이 직접 쓰는 문장 열한 칸이다(Mission/1Y/3Y/Gap/Bottleneck…).
지금은 **읽기 전용**이다. 고치려면 Supabase SQL Editor를 열어야 한다.

**왜 이렇게 됐나** 블록 6의 범위가 '보여 주는 것'이었다. RLS 정책(`business_strategy_write`)은
이미 승인권자에게 열려 있어서, 막힌 건 권한이 아니라 화면이다.

이게 D-10보다 성가신 이유: Bottleneck과 현재 우선순위는 **매달 바뀌는 값**이다.
고칠 수 없으면 한 달 뒤부터 이 패널은 틀린 말을 하게 된다.

**골라야 할 것**
- (A) 상세 화면에서 칸별 인라인 편집. 저장은 Server Action + `audit_log('update')`. 권장.
- (B) CH-002 기업 추가 같은 모달로 한 번에 열한 칸을 고친다. 만들기는 쉽지만 한 칸 고치는 데 폼 전체를 연다.
- (C) 그대로 두고 Chairman이 SQL로 갱신한다. 05_Architecture 6번(Production DB 직접 접근 금지)과 부딪힌다.

---

## D-14. 사이드바 메뉴 13개가 아직 빈 주소다

> **해소 (2026-09-07, Phase 1-D 블록 1) — 선택지 B.**
> 404 대신 `/coming-soon?menu=…` 한 화면. 메뉴 목록을 `lib/nav.ts`로 빼고
> `ready` 플래그로 갈랐다 — 사이드바와 '준비 중' 화면이 같은 목록을 봐야 하기 때문이다.
> 그 화면은 무엇을 기다리는지(대개 CH-052 ECOUNT / CH-053 미래소프트) 말하고
> 지금 쓸 수 있는 화면으로 되돌려 보낸다. 막다른 길로 두지 않는다.
> 같이: CH-049 `/settings/users`가 실제로 서면서 메뉴가 하나 늘었다(13 → 12개가 준비 중).

**무엇이** Phase 1-C로 살아 있는 화면은 대시보드 / 업무 관리 / 내 결정 사항(전자결재) /
문서 · 지식 / 회사 상세 다섯이다. 나머지 메뉴(그룹 전체 현황, AI 인사이트, 캘린더, 프로젝트,
기업 관리, 재무/회계, 인사/조직, 영업/CRM, 구매/SCM, 생산/MES, 연구/R&D, 자산/설비,
리스크/컴플라이언스, 설정)를 누르면 404다.

**왜 남겼나** 대부분 Layer 2(기능 시스템)라 Phase 2의 ECOUNT·MES·미래소프트 연동이 선행돼야 한다.
메뉴를 지우지 않은 건 05_Architecture의 모듈 경로를 화면 구조로 그대로 두기로 했기 때문이다.

**골라야 할 것**
- (A) 그대로 둔다. Phase 2 범위이고, 메뉴가 있어야 전체 구조가 보인다.
- (B) 아직 없는 메뉴를 눌렀을 때 404 대신 '준비 중' 화면을 낸다. 한 파일로 끝나고 오해가 줄어든다. 권장.
- (C) 미구현 메뉴를 사이드바에서 뺀다. 화면은 깔끔해지지만 전체 그림이 사라진다.

---

## D-09. live 모드에서 담당자 칸에 uuid가 그대로 보인다

**무엇이** Phase 1-B에서 live로 붙이고 나니 아래 세 곳이 36자 uuid를 그대로 뿌린다.

| 화면 | 지금 보이는 값 |
|---|---|
| CH-017 Waiting on Me · 담당 | `3d720a36-f18a-5182-85f4-a4b2da972b2b` |
| CH-012 Monthly Priority · 담당 | `6989f700-c2de-5e70-8a56-40847a6fc94d` |
| CH-014 Next Milestone · 담당 | `bc7f441e-0861-550f-986d-6f237144fc8d` |

**왜 이렇게 됐나** dummy일 때는 시드의 `owner`가 `user_001` 문자열이라 그게 그대로 떴다.
DB의 `owner_user_id`는 uuid 컬럼이라, `scripts/gen-seed-sql.ts`가 `user_001`을
sha1로 접어 uuid 모양으로 만들어 넣었다(`userUuid`). 그 uuid들은 **auth.users에 없는 값**이고
따라서 `user_profiles`에도 대응 행이 없다 — 즉 **이름을 꺼내 올 곳이 DB에 존재하지 않는다.**
어댑터(`repository/supabase.ts`)는 uuid를 그대로 `owner`에 넣고, 화면은 받은 문자열을 그린다.

`user_001`도 사람 이름은 아니었지만, 36자 uuid는 회장 화면에서 칸을 밀어내고 아무 정보도 주지 않는다.

**결정 (2026-09-06, Chairman): B.** 조인은 붙이고, 프로필이 없으면 '미지정'으로 떨어뜨린다.

- (A) 실제 임직원 계정을 만들고 `user_profiles`를 채운 뒤, 어댑터가 `owner_user_id → display_name`을
  조인해서 내린다. 근본 해결이지만 계정 생성이 선행돼야 한다.
- **(B) 선택.** 조인은 붙이되, 프로필이 없는 uuid는 '미지정'으로 떨어뜨린다.
  지금 시드로도 화면이 깨끗해지고, 계정이 생기는 순간 코드 수정 없이 (A)가 된다 —
  조인이 이미 붙어 있어 `user_profiles`에 행이 생기면 그때부터 이름이 뜬다.
- (C) 시드 uuid ↔ 표시명 매핑표를 앱에 하나 두고 그걸로 옮긴다.
  화면은 바로 좋아지지만 DB 밖에 두 번째 진실이 생긴다. 권하지 않는다.

**남은 일** 시드 담당자는 실계정이 아니라 계속 '미지정'으로 뜬다. 실제 이름이 필요해지면
임직원 계정을 만들고 시드의 `owner_user_id`를 그 uuid로 갈아야 한다(= A로 이어짐).

**주의** `user_profiles`는 `auth.users(id)`를 FK로 물고 있어(0002), 실재하지 않는 시드 uuid로는
프로필 행을 만들 수 없다. (A)를 고르면 시드의 `owner_user_id`도 같이 갈아야 한다.

---

## D-08. 기업 추가(CH-002)만 아직 브라우저에 남아 있다

**무엇이** Phase 1-B에서 결정 로그는 `audit_log`로, 숨김·핀은 `user_settings`로 옮겼다.
CH-002로 추가한 회사(`src/lib/added-businesses.ts`)만 localStorage에 그대로 있다.

**왜 남겼나** 성격이 다르다. 숨김·핀은 '내 화면' 설정이라 옮기는 데 개인 설정 표 하나면 됐다.
회사를 추가하는 건 조직 데이터를 만드는 일이라 세 가지가 같이 걸린다.

  1. `businesses` INSERT 권한 — 0002에서 Chairman만 갖는다. 지금은 맞지만 역할이 늘면 정책을 다시 봐야 한다.
  2. CH-051 감사 기록 — 회사 생성은 `create` 로 남아야 한다.
  3. `business_id` 발급 규칙 — 지금은 `biz_new_{timestamp}` 다. 서버가 발급하면 규칙을 정해야 한다.

지금 상태의 실제 증상: **추가한 회사는 그 브라우저에서만 보인다.** 다른 기기·다른 사람에게는 없다.
모달이 그 사실을 화면에 적어 두고 있다.

**골라야 할 것**
- **(A) 선택.** `businesses` INSERT + `audit_log('create')` 를 Server Action으로 붙인다.
- (B) Chairman이 Supabase Dashboard에서 직접 넣고, 화면의 추가 버튼은 뗀다.
- (C) 그대로 둔다(데모용으로만 본다).

**결정 (2026-09-06, Phase 1-C 블록 1): A.** 세 가지를 이렇게 정했다.

1. **권한** — 앱에서 역할을 보지 않는다. Chairman 세션으로 INSERT를 시도하고, 아니면
   0002의 `businesses_write`가 거부한다. 판정을 앱으로 옮기면 우회 경로가 하나 더 생긴다.
2. **감사 기록** — `audit_log`에 `create`를 먼저 남기고 그다음 INSERT한다. `recordDecisionAction`과
   같은 순서다. 반대로 두면 '회사는 생겼는데 만든 기록이 없는' 순간이 존재한다.
   중복 id 검사는 두 줄보다 앞에 둔다 — 입력 오류까지 기록에 남기면 기록이 잡음으로 찬다.
3. **id 발급 규칙** — `biz_` + 사람이 고른 영문 slug(`^[a-z][a-z0-9_]{1,29}$`). 시드의
   `biz_dy`/`biz_vana`를 그대로 잇는다. 한글 이름에서 로마자를 기계로 만들지 않는다 —
   '보람'이 boram인지 poram인지를 코드가 정하게 되기 때문이다. 이름에 영문이 있으면 초안만 채워 준다.
   규칙은 `lib/business-id.ts` 한 곳에 있고 모달과 Server Action이 같은 함수를 쓴다.

**남은 것** `owner_user_id`는 비워 둔다(화면에 '미지정'). 그 칸은 회사의 CEO이지 '만든 사람'이 아니라,
담당자를 고르는 UI가 생기기 전까지는 비우는 편이 정확하다.

---

## D-01. 그룹 EBITDA가 Revenue − Cost와 맞지 않는다 (CH-008)

**무엇이** 02_기능명세 CH-008은 "Revenue/Cost 기준 EBITDA 표시", Acceptance는 "정의된 Formula와 일치"다.
그런데 06_Dummy_Data의 2026-08 그룹 합계는 아래처럼 어긋난다.

| 항목 | 시드 합계 |
|---|---|
| Revenue | 66.3억 |
| Cost | 53.4억 |
| Revenue − Cost | 12.9억 |
| **EBITDA (시트값)** | **5.0억** |
| OperatingProfit (시트값) | 4.0억 |

**왜 막혔나** CLAUDE.md의 데이터 원칙이 "숫자 충돌 시 항상 시트가 이긴다"라, 지금 화면은 시트의
EBITDA 값을 그대로 표시하고 Formula로 재계산하지 않는다. 이 상태로는 CH-008 Acceptance를 만족했다고 말할 수 없다.

**골라야 할 것**
- (A) 시트값 유지 = 지금 상태. CH-008 Acceptance 문구를 "시트에 기록된 EBITDA를 그대로 표시"로 고친다.
- (B) 시드의 Cost가 매출원가만 담고 있다고 보고, EBITDA Formula에 쓸 비용 항목(판관비·감가상각)을
  06_Dummy_Data에 열로 추가한다. → 시드 수정이 필요하므로 내가 임의로 하지 않았다.
- (C) 화면에 Formula 검산 배지를 달아 "시트값 5.0억 / 계산값 12.9억 불일치"를 그대로 노출한다.

---

## D-02. Task에 '대기 시작 시각'이 없다 (CH-017)

**무엇이** CH-017 Waiting on Me는 "내 승인 때문에 Blocked된 업무"인데, 06_Dummy_Data의 Task에는
`deadline`만 있고 접수일·Blocked 전환 시각이 없다.

**왜 막혔나** 대기일수를 정확히 낼 수 없다. 지금은 **마감일이 지난 일수**를 대기일수로 쓰고,
마감 전이면 "대기 전"으로 표시한다(`waiting-on-me.tsx`의 `waitingDays`). 그래서 오늘 기준
7일 초과 경고가 한 건도 뜨지 않는다 — 로직이 아니라 데이터에 그 값이 없기 때문이다.

**골라야 할 것**
- (A) 02_데이터필드 Task에 `blocked_since` (ISO date, 필수) 추가 → 정확한 대기일수. 권장.
- (B) `created_at`만 추가하고 대기일수 = 오늘 − 생성일.
- (C) 지금처럼 마감일 기준으로 두고, 컬럼명을 "지연일"로 바꾼다.

---

## D-03. ②시안의 재무 숫자와 시드가 다르다 (CH-025~026)

**무엇이** ②시안 "그룹 전체 재무 현황"은 매출 202.8억 / 영업이익 19.9억 / EBITDA 20.0억 /
당기순이익 12.3억이고, x축이 5/1~5/26의 **일별**이다. 시드는 매출 66.3억이고 **월별 12개월**이다.

**왜 막혔나** CLAUDE.md 원칙대로 숫자는 시트를 따랐고, 축은 시드에 일별 데이터가 없어 월별 12개월로 그렸다.
즉 화면은 시안과 레이아웃만 같고 숫자·축이 다르다.

**골라야 할 것**
- (A) 시드 유지 = 지금 상태. 시안은 레이아웃 참고용으로만 본다.
- (B) 06_Dummy_Data에 일별 재무 시트를 추가해 시안과 축을 맞춘다.
- (C) 기간 드롭다운(현재 '이번 달', 동작 없음)에 일/월/분기를 붙이고 축을 전환한다. → CH-025 기간 필터와 같은 작업.

---

## D-04. 회사 KPI 목표(target)가 시드에 없다

**무엇이** 02_데이터필드 KPI에 `target`이 "선택"으로 정의돼 있고 타입에도 있지만, 06_Dummy_Data에는 값이 없다.

**왜 막혔나** CH-027 Budget vs Actual과 회사 카드의 목표 대비 진행률을 만들 수 없다.
지금 회사 카드의 진행률은 **그 회사가 굴리는 Project progress_pct의 평균**이다(`lib/finance.ts`의 `businessProgress`).
Chairman이 기대하는 "목표 대비 달성률"과 다른 숫자다.

**골라야 할 것**
- (A) 06_Dummy_Data finance-kpi에 target 열 추가.
- (B) Goal DB(CH-011)를 회사 단위까지 내려 회사별 Top Goal의 progress_pct를 쓴다.
- (C) 지금처럼 두고 카드 라벨을 "프로젝트 진행률"로 바꾼다.

---

## D-05. 개인 설정 4종이 전부 브라우저에만 저장된다

**무엇이** 숨김(CH-003) · 핀(CH-004) · 결정 로그(CH-016) · 추가한 기업(CH-002)이 모두 localStorage에 있다.

**왜 막혔나** 서버·DB·인증이 아직 없다. 특히 **결정 audit log**는 CH-051이 "삭제 불가"를 요구하는데,
브라우저 저장소는 사용자가 언제든 지울 수 있어 감사 요건을 만족하지 못한다.
(코드에는 삭제 함수를 만들지 않았고 append만 한다.)

**골라야 할 것**
- (A) Phase 1에서 Approval DB + Audit Log 테이블을 먼저 세운다. 권장 — CH-016은 그전까지 데모용으로만 본다.
- (B) 임시로 서버 없는 감사 로그를 파일 내보내기(JSON) 기능으로 보완한다.
- (C) 그대로 둔다.

---

## D-06. '그룹 전체 재무 현황' 제목이 두 곳에서 겹쳤다 (처리함, 확인 필요)

기존 8타일 KPI 스트립이 이미 "그룹 전체 재무 현황"이라는 제목을 쓰고 있었고,
②시안의 같은 이름 카드를 CH-025~026으로 새로 붙이면서 제목이 중복됐다.
→ 스트립 제목을 **"그룹 KPI (당월)"**로 바꾸고, "그룹 전체 재무 현황"은 시안대로 추이 카드가 가져갔다.
다른 이름을 원하면 `kpi-strip.tsx` 한 줄만 고치면 된다.

---

## D-07. 재무 추이 차트에서 손익 3선이 0 근처에 붙어 읽히지 않는다 (CH-025~026)

**무엇이** 한 축에 매출(66.3억)과 영업이익·EBITDA·당기순이익(2~5억)을 같이 그리면
아래 세 선이 0 위에 겹쳐 붙는다. 실제 스크린샷에서 세 선이 구분되지 않는다.

**왜 막혔나** 축을 둘로 나누는 건 "이 차트가 무엇을 비교하는 그림인가"를 바꾸는 결정이라
임의로 하지 않았다. 시안(②)은 매출 202.8억 / 손익 12~20억이라 비율이 3~16배였고, 시드는 13~33배다.

**골라야 할 것**
- (A) 이중 축: 왼쪽 매출, 오른쪽 손익 3종. 선 4개 유지. 권장.
- (B) 차트를 둘로 쪼갠다 — 매출 단독 + 손익 3선.
- (C) 손익을 매출 대비 %(마진율)로 바꿔 한 축에 올린다.
- (D) 그대로 둔다(매출 추세만 읽는 그림으로 본다).


---

## D-15. 초대 메일을 앱에서 직접 못 보낸다 (CH-049)

**무엇이** `/settings/users`가 초대를 저장하고 권한을 정하고 회수까지 하지만,
**메일은 보내지 못한다.** 회장이 초대를 누른 뒤 Supabase Dashboard →
Authentication → Users에서 그 주소로 계정을 한 번 만들어 줘야 한다.
계정이 생기는 순간 0011의 `on_auth_user_created` 트리거가 권한을 자동으로 붙인다.

**왜 이렇게 됐나** 보통은 `auth.admin.inviteUserByEmail()` 한 번으로 계정 생성과 메일이 같이
나간다. 그건 **service_role 키를 요구하고, 이 프로젝트에는 service_role이 없다**(CLAUDE.md).

서버에서 `signInWithOtp`로 우회하는 길도 막혀 있다. PKCE 검증자가 초대를 누른 **회장의**
브라우저 쿠키에 저장되는데, 링크를 여는 것은 **초대받은 사람의** 브라우저다. 검증자가 없어 링크가 열리지 않는다.

**지금 상태로 안전한 이유** 초대가 없는 이메일로 계정이 생기면 트리거가 아무것도 하지 않는다.
그 사람은 로그인은 되지만 `user_profiles`에 행이 없어 `currentUser()`가 null을 돌려주고,
RLS는 아무것도 내주지 않는다. 안전한 쪽으로 실패한다(0002 Default Deny).

**골라야 할 것**
- (A) 그대로 둔다. 사람이 자주 느는 조직이 아니고, 신원 발급은 Supabase가 하는 게 맞다.
  지금은 이걸 권한다.
- (B) service_role을 쓰는 Edge Function을 **하나만** 둔다(초대 전용). 키가 서버 밖으로 나가지 않고
  용도가 하나로 좁혀진다. CLAUDE.md의 'service_role은 없다'를 고쳐야 하므로 회장 결정이 필요하다.
- (C) Supabase에 SMTP를 직결하고 초대 메일 템플릿을 쓴다. 메일은 나가지만 계정 생성 시점을
  여전히 앱이 통제하지 못해 절차가 크게 줄지 않는다.

### 결정 (2026-09-07, Chairman) — **A. 수동 초대 + 트리거 연결**

초대 메일은 앱에서 보내지 않는다. service_role은 도입하지 않는다(CLAUDE.md 유지).
사람을 늘리는 절차는 두 단계로 고정한다.

1. `/settings/users`에서 초대를 저장한다 — 이메일·역할·접근 범위가 `user_profiles`에 먼저 앉는다.
2. Supabase Dashboard → Authentication → Users에서 **같은 주소로** 계정을 만든다.
   `on_auth_user_created`(0011)가 1번에서 저장한 행을 찾아 권한을 붙인다.

**순서가 바뀌면 안 된다.** 2번이 먼저면 트리거가 붙일 행이 없어 그 사람은 로그인은 되지만
`currentUser()`가 null을 돌려주고 RLS가 아무것도 내주지 않는다. 안전한 쪽으로 실패하지만
화면이 통째로 비어 보이므로 원인을 모르면 장애로 오해한다. 이때 복구는 1번을 다시 하고
그 사람이 재로그인하는 것이다.

**왜 A인가** 사람이 자주 느는 조직이 아니다. 신원 발급은 Supabase가 갖고 있는 게 맞고,
그걸 앱으로 끌어오려면 service_role 키가 프로젝트에 들어와야 한다. 초대 하나 편해지자고
전 테이블을 RLS 밖에서 만질 수 있는 키를 들이는 거래는 손해다.

**남는 비용** 사람을 늘릴 때마다 Dashboard를 한 번 거친다. 절차는 `docs/OPERATIONS.md`에 적는다.
**이 항목은 닫힌다** — 다시 열릴 조건은 '사람이 자주 는다'가 사실이 되는 때다.

---

## D-16. 문서 3종이 없다 (UAT TC-025)

**무엇이** 02_Definition_of_Done의 Documentation 항목은 '기능/DB/API/환경변수/수정가이드'
다섯을 요구한다. 지금 있는 것은 둘뿐이다.

| 요구 | 상태 | 어디에 |
|---|---|---|
| 기능 | 있음 | 02_기능명세 + 각 화면 파일 머리 주석 |
| DB | 있음 | `supabase/migrations/0001~0011` 머리 주석 |
| API | **없음** | Server Action 목록·입출력·실패 문장이 코드에만 있다 |
| 환경변수 | **없음** | `.env.example`에 값만 있고 '무엇이 왜 필요한가'가 없다 |
| 수정가이드 | **없음** | 새로 오는 사람이 어디부터 읽어야 하는지가 없다 |

**왜 급한가** UAT의 P0 Fail 일곱 중 **Phase 2를 기다릴 이유가 없는 유일한 항목**이다.
나머지 여섯은 ECOUNT·MES 연동이나 배포 환경이 선행되지만 이건 지금 쓸 수 있다.

**골라야 할 것**
- (A) `docs/` 아래 세 파일을 만든다. API는 Server Action 기준으로, 환경변수는 `.env.example`
  주석을 옮겨 확장, 수정가이드는 '화면 하나 고칠 때 어느 파일을 순서대로 여는가'. 권장.
- (B) README 하나로 합친다. 만들기는 빠르지만 셋의 독자가 다르다 —
  API는 개발자, 환경변수는 배포자, 수정가이드는 처음 오는 사람이다.
- (C) Phase 2로 미룬다. DoD를 못 채운 채로 Phase 1을 닫는다는 뜻이다.

---

## D-17. Staging 환경과 Rollback 절차가 없다 (UAT TC-023 · TC-024)

**무엇이** Supabase 프로젝트가 하나뿐이라 `.env.local` 한 벌로 live를 직접 본다.
Staging에서 뭔가 바꾸면 그게 곧 Production이다. 되돌리는 절차도 없다 —
마이그레이션에 down 스크립트가 없고, 배포본 자체가 아직 없다(로컬 `next dev`뿐).

**왜 이게 D-16보다 위인가** 배포를 하는 순간 이 둘이 없다는 사실이 처음으로 비싸진다.
0011까지 열한 개의 마이그레이션이 이미 원격에 적용됐고, 그중 하나를 되돌려야 하는 날이 오면
지금은 손으로 SQL을 쓰는 수밖에 없다. 그건 05_Architecture 6번과 정면으로 부딪힌다.

**골라야 할 것**
- (A) Supabase 프로젝트를 하나 더 만들어 Staging으로 쓰고, 마이그레이션은 거기서 먼저 돌린다.
  비용이 늘고 시드를 두 번 관리해야 하지만 TC-023이 요구하는 게 정확히 이것이다.
- (B) 같은 프로젝트에 스키마를 나눈다(`staging` 스키마). 싸지만 RLS 정책을 두 벌 관리해야 하고,
  실수로 Production 스키마를 건드릴 경로가 남는다.
- (C) Staging 없이 가되 **Rollback만** 먼저 갖춘다. 마이그레이션마다 down을 쓰고,
  배포는 Vercel의 이전 배포로 되돌린다. TC-024만 닫고 TC-023은 열어 둔다.

**참고** 어느 쪽을 고르든 마이그레이션 down 스크립트는 필요하다.
0011까지 열한 개가 전부 up만 갖고 있다.

---

## D-18. 업무의 담당자·제목·마감을 여기서 못 고친다 (D-12에서 갈라져 나옴)

**무엇이** `/tasks/[id]`가 바꿀 수 있는 것은 **상태**와 **회장 확인** 둘뿐이다.
담당자는 보여 주기만 한다. 제목·마감도 마찬가지다.

**왜 이렇게 됐나** 의도된 경계다. `TaskPatch`(repository/types.ts)가 처음부터 두 칸만 받는다:

> 제목·담당자·마감은 없다. 이 화면은 '지금 어떻게 되고 있나'를 옮기는 자리지
> 업무를 편집하는 자리가 아니다 — 편집은 Business OS(Layer 1)의 일이다.

Chairman OS는 Layer 2(그룹 관제)다. 업무의 내용을 쓰는 곳은 각 회사의 Business OS이고,
여기는 그걸 모아 보고 **결정하는** 자리라는 게 05_Architecture의 층 나눔이다.

**언제 다시 열리나** '회장이 담당자를 직접 바꾼다'가 실제 업무 흐름이 될 때다.
그때 고쳐야 할 것은 화면 하나가 아니다 — `TaskPatch`, 0002의 `tasks_write` 정책,
그리고 Layer 1과의 역할 분담 문서까지 같이 움직인다.

**골라야 할 것**
- (A) 그대로 둔다. 층 나눔을 지킨다. 지금은 이걸 권한다.
- (B) 담당자만 연다. `TaskPatch`에 `owner_user_id`를 더하고 승인권자에게만 허용한다.
      한 칸이지만 '여기서도 업무를 편집한다'는 선례가 된다.
- (C) 편집 화면을 통째로 연다. Layer 1이 무엇을 하는 곳인지 다시 정의해야 한다.
